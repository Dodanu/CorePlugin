package core.main.core.Message;

import java.util.HashMap;
import java.util.UUID;

public class MessageManager {

    public static HashMap<UUID, UUID> messages = new HashMap<UUID, UUID>();

}
